[project]
name = "spacetraders-ctl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "redjax", email = "no@none.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
spacetraders-ctl = "spacetraders_ctl:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
